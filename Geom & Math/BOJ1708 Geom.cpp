#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;

int N;
struct point { int x, y, p = 0, q = 0; };
point p[100000];
vector<int> V;

bool sortp(point A, point B) {

}
long long ccw(point A, point B, point C) {
	
}
int main() {
	scanf("%d", &N);
	for (int i = 0; i < N; ++i) scanf("%d%d", &p[i].x, &p[i].y);


}